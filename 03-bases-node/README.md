# Notas:

Primer progama en NODE.js

```
Options:
  --help     Show help                                                 [boolean]
  --version  Show version number                                       [boolean]
  -b, --base     escoge la tabla que quieres crear           [number] [required]
  -l, --listar  muestra o no la tabla creada          [boolean] [default: false]
  -h, --hasta  Hasta que numero se hara la tabla creada                 [number]
```